import styles from './produk.module.css';

const MainSection = () => {
    return (
        <main className={styles.main}>
            <p>Konten list produk</p>
        </main>
    );
};

export default MainSection;
